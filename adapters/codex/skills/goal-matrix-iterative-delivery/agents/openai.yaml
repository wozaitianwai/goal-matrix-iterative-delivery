interface:
  display_name: "Goal Matrix Delivery"
  short_description: "Goal matrix execution with tight scope control"
  default_prompt: "Use goal-matrix-iterative-delivery: the first substantive response must show a goal matrix or active-goal block before freeform discussion, then execute one active child goal, verify it, and keep scope minimal."
policy:
  allow_implicit_invocation: false
